# Thu vien cho HTTP Server (chay tren MAY TINH, khong phai tren Pi).
#
# Cai dat:
#     python3 -m venv .venv
#     .venv/bin/pip install -r server/requirements.txt

fastapi>=0.115          # framework HTTP + tu sinh tai lieu API o /docs
uvicorn[standard]>=0.30 # may chu ASGI chay FastAPI
motor>=3.6              # driver MongoDB bat dong bo (dung chung voi async cua FastAPI)
pydantic>=2.9           # validate du lieu vao/ra
pydantic-settings>=2.5  # doc cau hinh tu file .env
python-multipart>=0.0.9 # BAT BUOC de FastAPI doc duoc form-urlencoded
                        # (thieu no thi de bai "json hoac form-urlencoded" khong chay)

# Chi can khi chay test
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27             # goi API trong test ma khong can chay server that
