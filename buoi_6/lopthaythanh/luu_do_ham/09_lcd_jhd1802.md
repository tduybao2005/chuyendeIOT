# Lưu đồ LCD — `show_lcd()` (dòng 309) và class `JHD1802` (dòng 263)

```mermaid
flowchart TD
    START([Bắt đầu show_lcd<br/>mode, led_on, buzzer_on, relay_on]) --> NOW["now_str = giờ:phút:giây<br/>hệ thống hiện tại"]
    NOW --> MODESTR{"mode == 'manual'?"}
    MODESTR -- "Có" --> MANU["mode_str = 'MANU'"] --> ROW0
    MODESTR -- "Không" --> AUTO["mode_str = 'AUTO'"] --> ROW0
    ROW0["setCursor(0, 0)<br/>write('Time HH:MM:SS')"]
    ROW0 --> ROW1["setCursor(1, 0)<br/>write('MODE L{0/1} B{0/1} R{0/1}')"]
    ROW1 --> END([Kết thúc])
```

```mermaid
flowchart TD
    S2([Khởi tạo JHD1802<br/>__init__]) --> BUS["Mở SMBus(1)<br/>(I2C bus 1 của Pi)"]
    BUS --> TRY{"Thử cấu hình<br/>đèn nền + màn hình?"}
    TRY -- "Lỗi (Exception)" --> LOGI["In lỗi khởi tạo<br/>(không dừng chương trình)"] --> ENDI([Kết thúc khởi tạo])
    TRY -- "OK" --> RGB["_set_rgb_backlight(255,255,255)<br/>ghi 6 byte qua I2C 0x62"]
    RGB --> CMD1["textCommand(0x38)<br/>function set"]
    CMD1 --> CMD2["textCommand(0x0C)<br/>bật hiển thị"]
    CMD2 --> CMD3["textCommand(0x01)<br/>xoá màn hình"]
    CMD3 --> SLP["sleep(0.1s)"]
    SLP --> ENDI

    subgraph WRITE["write(msg) — dòng 298"]
        direction TD
        W0([Bắt đầu]) --> WL{{"Với mỗi ký tự c<br/>trong msg"}}
        WL --> WB["write_byte_data(0x3E, 0x40, ord(c))<br/>qua I2C"]
        WB --> WE{"Lỗi ghi I2C?"}
        WE -- "Có" --> WSKIP["Bỏ qua ký tự này<br/>(nuốt lỗi, không dừng)"] --> WL
        WE -- "Không" --> WL
        WL -- "Hết chuỗi" --> WEND([Kết thúc])
    end
```

**Vì sao mọi thao tác I2C đều bọc `try/except` và nuốt lỗi?** Màn hình
LCD/đèn nền chỉ là thiết bị hiển thị phụ — nếu dây I2C lỏng hoặc màn hình
hỏng, chương trình vẫn phải tiếp tục đọc cảm biến, điều khiển thiết bị và
gửi dữ liệu lên Server bình thường, không được để một khối hiển thị làm treo
toàn bộ chương trình.
